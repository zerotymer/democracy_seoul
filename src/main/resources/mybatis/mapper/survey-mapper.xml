<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="proposal">
	<!-- SQL QUERYS -->
	
<select id="survey" resultType="kr.go.seoul.democracy.survey.vo.Survey">
 SELECT *
 	survey_no as surveyNo,
	proposal_no as proposalNo,
	survey_title as surveyTitle
 FROM Survey join Proposal 
 on Survey.proposalNo = proposal.ProposalNo 
</select>
	
	
</mapper>
	