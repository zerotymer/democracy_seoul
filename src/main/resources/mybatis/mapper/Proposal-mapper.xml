<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mybatis">
	<!-- SQL QUERYS -->
	
	<select id="selectAllList" resultType="proposal">
	SELECT 
			proposal_no as proposalNo,
 		 	proposal_title as proposanlTitle,
 		 	proposal_content as proposalContent,
 		 	proposal_date as proposalDate,
 		 	proposal_start as proposalStart,
 		 	proposal_end as proposalEnd,
 		 	proposal_del_YN as proposalDelYN,
 		 	proposal_thumbnail as proposal_Thumbnail
 		 	proposal_thumbnail_path as proposal_ThumbnailPath
	FROM 
		PROPOSAL
	</select>
	
</mapper>
	